# Enemy sprites (battle)

Place one texture per enemy. Names must match **Enemies** in `Database/Enemies.cs` (e.g. `Slime`, `Bat`, `Skeleton`).

Add to `SharpSixteen.mgcb` under `assets/enemies/<Id>.png`.  
Fallback: `Sprites/Jrpg/EnemySlime.png`.
