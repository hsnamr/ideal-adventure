# Character sprites (overworld / battle)

Place 16×16 (or compatible) character sprites here. The game loads by name:

- **Hero** – player overworld/battle
- **Npc** – NPCs

Add to `SharpSixteen.mgcb` under `assets/characters/<Name>.png`.  
Fallback: `Sprites/Jrpg/<Name>.png`.
